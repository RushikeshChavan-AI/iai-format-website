{
  "name": "iai-format-website",
  "version": "0.1.0",
  "private": true,
  "description": "Static marketing site for IAI Studio.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npx serve .",
    "preview": "npx serve dist"
  },
  "engines": {
    "node": ">=20"
  }
}
